/**
 * El Estratega — cruza ángulos × formatos × canales.
 *
 * STUB. Se implementa en el worktree `pipeline`.
 *   Reglas de config/darwin.config.ts: 1 ad = 1 ad set, budget_per_adset_usd,
 *   lane_pct_max, las 3 tiers de kill_rules y el umbral de graduación.
 *   `memory` viene null en la primera corrida → memory_applied queda [].
 *   PROHIBIDO pronosticar CAC: success_metrics dice qué se mide, no qué pasará.
 */
import { NotImplemented, type StrategistFn } from "../contract";

export const strategist: StrategistFn = async () => {
  throw new NotImplemented("El Estratega", "pipeline");
};
