/**
 * Banco de ángulos — LA MECÁNICA NÚCLEO.
 *
 * STUB. Se implementa en el worktree `pipeline`.
 *   Invierte la frase del cliente en promesa:
 *     "se destapa toda la noche" → "la cobijita que sí se queda puesta"
 *   Cada ángulo arrastra el `source_quote` del insight que lo originó. Zod lo
 *   exige y el war room lo pinta como el elemento más grande de la tarjeta.
 */
import { NotImplemented, type AnglesFn } from "../contract";

export const angles: AnglesFn = async () => {
  throw new NotImplemented("Banco de ángulos", "pipeline");
};
