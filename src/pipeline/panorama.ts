/**
 * Panorama — qué vende la marca y qué formato le rinde.
 *
 * STUB. Se implementa en el worktree `pipeline`.
 *   Lee: ctx.data.site_text, ctx.data.posts, data/category-benchmarks.json
 *   Debe: correr las 4 fuentes con Promise.allSettled + timeout y registrar
 *         coverage[].status por cada una. Ninguna fuente es requisito.
 */
import { NotImplemented, type PanoramaFn } from "../contract";

export const panorama: PanoramaFn = async () => {
  throw new NotImplemented("Panorama", "pipeline");
};
